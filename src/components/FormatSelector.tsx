import { ALL_FORMATS, COMMON_FORMATS, getFormatById } from '../formats';
import { useTranslation } from '../i18n';
export default function FormatSelector({ value, onChange, disabled }: { value: string; onChange: (format: string) => void; disabled?: boolean }) { const { t } = useTranslation(); return <div className="w-full"><label className="mb-2 block text-sm text-gray-700 dark:text-gray-200">{t('formats.selectOutput')}</label><select value={value} onChange={(e)=>onChange(e.target.value)} disabled={disabled} className="w-full rounded border border-gray-300 bg-white px-3 py-2 text-sm text-gray-800 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100"><optgroup label={t('formats.common')}>{COMMON_FORMATS.map((id)=>{ const f=getFormatById(id); return f?<option key={id} value={id}>{f.label}</option>:null; })}</optgroup>{ALL_FORMATS.map((g)=><optgroup key={g.label} label={t(`formats.groups.${g.label}`)}>{g.formats.filter((f)=>f.canOutput).map((f)=><option key={f.id} value={f.id}>{f.label}</option>)}</optgroup>)}</select></div>; }
