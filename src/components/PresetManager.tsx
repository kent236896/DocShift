import { useState } from 'react';
import { useTranslation } from '../i18n';
import { useAppStore } from '../store';
export default function PresetManager({ fromFormat, toFormat }: { fromFormat: string; toFormat: string }) { const { t } = useTranslation(); const presets = useAppStore((s) => s.presets); const add = useAppStore((s) => s.addPreset); const del = useAppStore((s) => s.deletePreset); const apply = useAppStore((s) => s.applyPreset); const st = useAppStore((s) => s.settings); const [name, setName] = useState(''); return <section className="rounded border border-gray-200 bg-white p-3 dark:border-gray-700 dark:bg-gray-800"><h3 className="mb-2 text-sm font-semibold text-gray-800 dark:text-gray-100">{t('presets.title')}</h3><div className="mb-3 flex gap-2"><input value={name} onChange={(e)=>setName(e.target.value)} placeholder={t('presets.namePlaceholder')} className="flex-1 rounded border border-gray-300 bg-white px-2 py-1 text-sm text-gray-800 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-100"/><button onClick={()=>{ if(!name.trim()) return; add({ name: name.trim(), fromFormat, toFormat, outputDir: st.outputDir==='custom'?st.customOutputDir:null, pandocArgs: st.pandocExtraArgs }); setName(''); }} className="rounded bg-brand-600 px-3 py-1 text-sm text-white">{t('presets.save')}</button></div>{presets.length===0?<p className="text-xs text-gray-500 dark:text-gray-400">{t('presets.empty')}</p>:<div className="space-y-2">{presets.map((p)=><div key={p.id} className="flex items-center justify-between rounded border border-gray-200 px-2 py-1 dark:border-gray-700"><div><p className="text-sm text-gray-800 dark:text-gray-100">{p.name}</p><p className="text-xs text-gray-500 dark:text-gray-400">{p.fromFormat} ¡ú {p.toFormat}</p></div><div className="flex items-center gap-1"><button onClick={()=>apply(p)} className="rounded px-2 py-1 text-xs text-blue-700 hover:bg-blue-100 dark:text-blue-300 dark:hover:bg-blue-900/30">{t('presets.apply')}</button><button onClick={()=>del(p.id)} className="rounded px-2 py-1 text-xs text-red-700 hover:bg-red-100 dark:text-red-300 dark:hover:bg-red-900/30">¡Á</button></div></div>)}</div>}</section>; }
