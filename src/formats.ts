import type { FormatDef, FormatGroup } from './types';
export const ALL_FORMATS: FormatGroup[] = [{ label: 'documents', formats: [{ id: 'docx', label: 'Word (.docx)', extensions: ['docx'], canInput: true, canOutput: true }, { id: 'odt', label: 'OpenDocument (.odt)', extensions: ['odt'], canInput: true, canOutput: true }, { id: 'pdf', label: 'PDF (.pdf)', extensions: ['pdf'], canInput: true, canOutput: true }, { id: 'epub', label: 'EPUB (.epub)', extensions: ['epub'], canInput: true, canOutput: true }, { id: 'rtf', label: 'RTF (.rtf)', extensions: ['rtf'], canInput: true, canOutput: true }] }, { label: 'markup', formats: [{ id: 'markdown', label: 'Markdown (.md)', extensions: ['md', 'markdown'], canInput: true, canOutput: true }, { id: 'html', label: 'HTML', extensions: ['html', 'htm'], canInput: true, canOutput: true }, { id: 'rst', label: 'reStructuredText', extensions: ['rst'], canInput: true, canOutput: true }, { id: 'latex', label: 'LaTeX (.tex)', extensions: ['tex'], canInput: true, canOutput: true }, { id: 'org', label: 'Org-mode', extensions: ['org'], canInput: true, canOutput: true }, { id: 'textile', label: 'Textile', extensions: ['textile'], canInput: true, canOutput: true }, { id: 'asciidoc', label: 'AsciiDoc', extensions: ['adoc', 'asciidoc'], canInput: true, canOutput: true }, { id: 'mediawiki', label: 'MediaWiki', extensions: ['wiki'], canInput: true, canOutput: true }] }, { label: 'presentations', formats: [{ id: 'pptx', label: 'PowerPoint (.pptx)', extensions: ['pptx'], canInput: true, canOutput: true }, { id: 'beamer', label: 'LaTeX Beamer', extensions: ['tex'], canInput: false, canOutput: true }, { id: 'revealjs', label: 'Reveal.js HTML', extensions: ['html'], canInput: false, canOutput: true }] }, { label: 'data', formats: [{ id: 'json', label: 'JSON', extensions: ['json'], canInput: true, canOutput: true }, { id: 'csv', label: 'CSV', extensions: ['csv'], canInput: true, canOutput: false }, { id: 'docbook', label: 'DocBook XML', extensions: ['xml'], canInput: true, canOutput: true }, { id: 'jats', label: 'JATS XML', extensions: ['xml'], canInput: true, canOutput: true }, { id: 'man', label: 'Man page', extensions: ['man'], canInput: true, canOutput: true }] }];
export const COMMON_FORMATS: string[] = ['docx', 'pdf', 'markdown', 'html', 'epub', 'pptx'];
const ALL = ALL_FORMATS.flatMap((g) => g.formats);
export const ALL_INPUT_FORMAT_IDS: string[] = [...new Set(ALL.filter((f) => f.canInput).map((f) => f.id))];
export const ALL_OUTPUT_FORMAT_IDS: string[] = [...new Set(ALL.filter((f) => f.canOutput).map((f) => f.id))];
export function getFormatById(id: string): FormatDef | undefined { return ALL.find((f) => f.id === id); }
export function detectFormatFromExtension(filename: string): string | undefined { const ext = filename.split('.').pop()?.toLowerCase(); if (!ext) return undefined; return ALL.find((f) => f.canInput && f.extensions.includes(ext))?.id; }
